# Task 3: console integration

Own only management-console and platform-admin-console in /Users/tjtanjin/Desktop/acornops/hosted-readiness, plus docs/hosted-task-3-report.md. Read AGENTS/applicable skills, especially PAC local platform-admin-change. No subagents or commits/push. Approved plan docs/oss-hosted-readiness-plan.md. Root owns CP/contracts deployment.

Management console:
- CP GET /api/v1/workspaces?view=access-state returns {items:[{id,name,accessState:'active'|'suspended',publicReason?,suspendedAt?}]} for current user's memberships. GET /api/v1/workspaces/:workspaceId/access-state returns same single row; missing/nonmember404. Safe projection no permissions/targets/counts/plan/private reasons. Normal workspace list remains active-only.
- Load minimal discovery before workspace workload effects; show suspended workspace selector items without pretending they have normal data permissions. Deep links can resolve safe state independently of active list.
- Blocking accessible modal/page with workspace name and publicReason, switch workspace/back to workspace directory/signout. No private reasons, billing/credits/upgrade marketing, no adminrestore UI.
- Guard before workload children mount and all App/bootstrap/cache/global effects prefetch. Existing mounted workload unmounts and caches/streams clear on suspension. Never render sensitive data behind modal. HTTP403 WORKSPACE_SUSPENDED and SSE workspace_suspended event trigger fresh safe state; bounded periodic access-state revalidation handles idle tabs missed streams. Restore explicit refresh/load current state; do not replay old actions.
- Maintain normal self-hosted active flows and mock mode fixtures. Targeted browser test asserts zero target/run/tool/network workload requests while suspended, deep link/login selector/currenttab suspension, restoration fresh navigation. Validate VITE_APP_DATA_MODE=control-plane npm run validate and affected Playwright specs. No whole-app design overhaul.

PAC:
- Update accepted current requirements + executable baseline to permit readonly quotaOverrides/effective limits and execution usage; override editing remains API-only. REQ-WSP002/EXC015 previously prohibited override exposure: accepted design replaces this only for readonly administrative summary. REQ-WSP004/EXC017 deterministic action reasons and exact confirmation preserved; no freeform private reason editor.
- CP GET /admin/v1/workspace-plans -> {defaultPlanKey,plans}; each plan{name,key,limits resource3,executionLimits five}. GET /admin/v1/workspaces/:workspaceId/policy -> {workspaceId,workspaceName,plan,effectiveLimits,quotaOverrides,policyVersion,lifecycleStatus,suspendedAt,usage:{members,kubernetesClusters,virtualMachines,execution:{chat,agent,workflow,autoTriage,insights}:{concurrentRuns,outstandingRuns}},holds:[{source:'admin'|'external',publicReason,suspendedAt}],publicReason?}. Read source CP repository-workspace-policy-read.ts for exact effective fields. Null pair displays unlimited. Do not expose tenant run lists/logs.
- Existing plan mutation supports {planKey,reason,requestId,expectedPolicyVersion,overLimitBehavior:'reject'|'retain_existing'}. preserve overrides default. Use unique request IDs with observedversion; retry same exact payload reusesID, staleversion refetch policy and new deliberate request. Downgrade conflict requires explicit retain_existing confirmation without deleting work/resources.
- Suspend/restore {workspaceName,reason,source:'admin',requestId,expectedPolicyVersion}; suspend requires exact name, retainexisting confirmations. PAC manipulates adminhold only. External hold shown separately; restoring admin while external remains must truthfully display stillsuspended. Operator private reason deterministic existing taxonomy; publicReason generic or existing approved safe strings.
- Browser only sameorigin BFF. Update BFF allowed route contract, input schema, responsepick projections (existing before/after policy/changed fields currently discarded), mocks and tests. Authscopes existinghuman, MFA/CSRF/recentauth preserved. No machine token frontend.
- Run ADMIN_CONSOLE_DATA_MODE=control-plane validate plus requirements/contracts/harness/smoke:routes and affected browsertest. Report evidence/limitations.
