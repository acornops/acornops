# Task 2: Python execution authority and gateway boundary

Work only in `/Users/tjtanjin/Desktop/acornops/hosted-readiness/execution-engine` and `/Users/tjtanjin/Desktop/acornops/hosted-readiness/llm-gateway`. Read their AGENTS and applicable skills. No subagents, no commits/push. Root owns control-plane and later UI/deployment. Implement, test, report.

Approved scope: five independent workspace pools (chat,agent,workflow,autoTriage,insights), two counters each. CP is authoritative and server-classifies attempts. Engine global MAX_CONCURRENT_RUNS remains independent. No billing/rates/new queue product.

CP contract implemented in src/controllers/internal-run-capacity-controller.ts and services/workspace-execution-access.ts (read those files for exact current schemas):
- POST /internal/v1/runs/:runId/capacity/acquire {ownerId} -> {status:granted|wait|blocked|disabled, generation?,pool?,leaseSeconds?,contractVersion:1}.
- renew {ownerId,generation}; release {ownerId,generation,state:parked|settling} -> status ok. disabled skips capacity but still checks lifecycle on noncleanup.
- operations/begin {ownerId,generation,operationId,timeoutMs}, operations/finish {ownerId,generation,operationId}. Both authenticated with existing CP service token. Begin rejects replay; finish idempotent even after suspension/lease expiry.
- Existing /internal/v1 run routes now require `x-acornops-execution-owner` and `x-acornops-execution-generation` while capacity enabled. Cleanup callbacks require same generation/owner but may settle expired/suspended work. All work boundaries check lifecycle even while capacity disabled.
- New CP service endpoint for gateway lifecycle/operation authorization will be coordinated with root. Gateway should authenticate its existing run JWT first, use verified claims.run_id/workspace_id and forwarded owner/generation header, not request-supplied workspace identity. For enabled runs call begin/finish around actual upstream provider/MCP dispatch. Never retry same operation ID or writes after uncertain timeout. Existing MCP user generation/approvals/token scopes preserved.

Implementation:
1. Add WORKSPACE_CAPACITY_ENABLED bool defaultfalse in both settings. Dispatch includes capacity_contract_version:1 and capacity_enabled (root adds). In enabled mode reject missing/incompatible dispatch contracts or mismatching mode; advertise capability in health. In disabled mode retain old dispatch compatibility.
2. Engine capacity manager owns per-task context owner random worker ID + generation; acquire before bootstrap/active work, renew10s (lease30s CP), release after task unwinds in finally. If acquire wait, leave bounded outstanding work queued without holding local semaphore. Queue expiry600s. Stop initiating work after authority loss. Context headers must reach bootstrap/context/continuations/events/commit and gateway LLM/tool clients. Cleanup flush must retain authority identity for durable event/commit retries.
3. Fix run_loop draining unbounded tasks. Bound runnable tasks to worker max; retain durable queued work and avoid a saturated workspace blocking all workspaces. Existing Redis durability/run-id dedupe preserved. Existing Redis locks must not replace CP authority or unconditionally delete a different owner's lock; use owner-aware operations where needed.
4. Coordinator dependency wait currently polls via CoordinationToolClient._acornops_await_delegations; implement durable continuation rather than polling model while retaining grants. CP will expose POST /internal/v1/runs/:runId/dependency-wait {generation,state} storing ReAct transcript, and GET existing continuation returns discriminated dependency continuation after children settled. Coordinate exact shape with root. On pending child: emit suspension with serialized continuation and release local/distributed gates. Resume same attempt later through existing run registry resumption path; inject child results without replaying delegation writes. Required child capacity denial must yield explicit terminal workflow outcome.
5. Gateway every provider and MCP dispatch validates active lifecycle even disabled. Enabled mode registers operation before actual upstream request, finishes when bounded upstream request ends (also errors/cancellation). Forward context through builtin MCP/control-plane hop without registering duplicate operation there. Valid old JWT is insufficient after suspension/lost generation. Hosted BYOK behavior remains unchanged: workspace secret then optional self-hosted default; no new payment behavior.
6. Tests first: acquire/wait/loss/cancel/cleanup, bounded queues, approval park/resume, parent-child concurrency1, gateway denied operation makes zero provider/tool calls, workspace key isolation/fallback regression, missing capacity contract. Run full task validate in both existing Python3.12.11 .venv (available via symlink). Do not spend live keys; deterministic mocks only.

Report exact changed files, CP contract needs, test results and any concerns in `/Users/tjtanjin/Desktop/acornops/hosted-readiness/docs/hosted-task-2-report.md`. Do not claim distributed acceptance from unit tests; root runs real DB/replicas later.

Gateway authority endpoint now defined: POST /internal/v1/runs/:runId/capacity/authorize {workspaceId} using ORCH_SERVICE_TOKEN. Verifies persisted identity and lifecycle/cancellation even disabled; returns {status:'ok',capacityEnabled:boolean,contractVersion:1}. Gateway takes both IDs only from verified JWT claims. Then begin/finish enabled operations as above. CP middleware now prioritizes verified gatewayRunClaims.runId for MCP bridge.

CP dependency continuation side implemented: POST dependency-wait as above returns204; GET existing continuation checks dependency first, returns {kind:'dependency',runId,generation,state}; DELETE existing continuation clears both kinds. This uses persisted workspace_run_reservations state parked; enabled engine must release parked AFTER durable save and unwinding. Disabled CP saves dependency reservation as parked itself. CP maintenance redispatches same run while parked and all children terminal; registry must accept same identity from its dependency-wait state. Parent workflow_run remains running during dependency wait (no new public run-status enum); reservation state drives accounting. Root adding maintenance resumption and tests.
